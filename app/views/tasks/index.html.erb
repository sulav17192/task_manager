<h2>Tasks</h2>

<% if policy(Task).create? %>
  <%= link_to "New Task", new_task_path %>
<% end %>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task %>
      — due <%= task.due_date %>
      — status: <%= task.status %>
    </li>
  <% end %>
</ul>
