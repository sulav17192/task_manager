<%= form_with(model: task, local: true, class: "needs-validation") do |f| %>
  <% if task.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(task.errors.count, "error") %> prevented this task from being saved:</h4>
      <ul>
        <% task.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :title, class: "form-label" %>
    <%= f.text_field :title, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :description, class: "form-label" %>
    <%= f.text_area :description, class: "form-control", rows: 4 %>
  </div>

  <div class="mb-3">
    <%= f.label :due_date, class: "form-label" %>
    <%= f.date_field :due_date, class: "form-control" %>
  </div>

  <% if current_user.admin? || current_user.manager? %>
    <div class="mb-3">
      <%= f.label :assigned_to_id, "Assign to", class: "form-label" %>
      <%= f.collection_select :assigned_to_id, User.where(role: :member), :id, :email, {}, { class: "form-select" } %>
    </div>
  <% end %>

  <%= f.submit task.new_record? ? "Create Task" : "Update Task", class: "btn btn-primary" %>
<% end %>
